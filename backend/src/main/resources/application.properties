spring.application.name=new_web_ide

server.port=8080
# ===============================
# = Database (MySQL) ??
# ===============================
spring.datasource.url=jdbc:mysql://app_db:3306/IDE_DB?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
spring.datasource.username=chiyomomo
spring.datasource.password=shamimomo56562
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# ===============================
# = Redis
# ===============================
spring.data.redis.host=localhost
spring.data.redis.port=6379

# ===============================
# = Spring Batch
# ===============================
spring.batch.initialize-schema=always
spring.batch.job.enabled=true
logging.level.org.springframework.batch=INFO